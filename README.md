# Lab 5 - Starter
Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!

Name: Nhan Tri Danh

1) Would you use a unit test to test the “message” feature of a messaging application? Why or why not? For this question, assume the “message” feature allows a user to write and send a message to another user.
- No I would not since this scope here is still a bit too wide for unit test. If I wanted to test it, I would test smaller part of it such as functions that validates input message is not empty, function that format message before sending, function that update UI when a message is recieved..etc..

2) Would you use a unit test to test the “max message length” feature of a messaging application? Why or why not? For this question, assume the “max message length” feature prevents the user from typing more than 80 characters.
- Yes I would since this feature is small and encapsulated enough to test, intuitively this can be code with a function only.


Screenshots:
1) myError.error with equal 3 instead of 4
![alt text](myError.error.png)
2) merged.correct after fix
![alt text](merged.correct_after_fix.png)
